import { TransformControls } from 'three/examples/jsm/controls/TransformControls.js';

export class GeometryControls extends TransformControls {
    constructor(object,domElement) {
        super(object,domElement);
    }
}